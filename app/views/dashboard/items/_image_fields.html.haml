#image
  = image_tag(f.object.filename_url(:thumb)) if f.object.filename?
  = f.file_field :filename
  -if f.object.new_record?
    = remove_child_link(t(:remove_field), f)
  -else
    = f.check_box :_destroy
    = f.label :_destroy, t(:remove)